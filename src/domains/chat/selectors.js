const username = (state) => state.chat.username;
const members = (state) => state.chat.members;
const messages = (state) => state.chat.messages;

export default {
  username,
  members,
  messages
};
